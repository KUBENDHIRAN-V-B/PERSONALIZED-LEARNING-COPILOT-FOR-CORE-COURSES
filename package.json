{
  "name": "personalized-learning-copilot",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "npm install --prefix backend && npm install --prefix frontend && cd frontend && npm run build",
    "build": "npm install --prefix backend && npm install --prefix frontend && cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "dependencies": {},
  "devDependencies": {}
}